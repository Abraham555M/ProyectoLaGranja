<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado seleccionado -->
    <item android:state_checked="true" android:drawable="@color/color_secundario" />
    <!-- Estado normal -->
    <item android:drawable="@android:color/transparent" />
</selector>
